  <goods
    app-id="{{ appId }}"
    shop-id="{{ shopId }}"
    open-id="{{ openId }}"
    goods-from="{{ 0 }}"
    layout="{{ 0 }}"
    goods-ids="{{ 449972146 }}"
    size-type="{{ 3 }}"
    extraData="{{extraData}}"
    redirect-type="0"
  />
