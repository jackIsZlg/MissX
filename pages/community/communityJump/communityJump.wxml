  <goods
    app-id="{{ appId }}"
    shop-id="{{ shopId }}"
    open-id="{{ openId }}"
    goods-from="{{ 0 }}"
    layout="{{ 0 }}"
    goods-ids="{{ 449971428 }}"
    size-type="{{ 7 }}"
    extraData="{{extraData}}"
    redirect-type="0"
    textAlignType="{{ 'center' }}"
    showBuyButton="{{ false }}"
    borderRadiusType="{{ 2 }}"
  />
